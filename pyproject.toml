[project]
name = "censoring-generation"
version = "0.1.0"
description = "Synthetic and semi-synthetic censoring generation for survival analysis experiments"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "numpy",
    "pandas",
    "lifelines",
    "scipy",
    "copulas",
]

[tool.uv]
package = false
